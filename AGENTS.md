# AGENTS.md

이 레포(`snas-lifebook/decline-and-fall-of-the-roman-empire`, public)는 에드워드 기번 『로마제국쇠망사』 관련 원문 자료와 온톨로지를 담은 저장소다. 한국어 편역본(30포인트), 기번 영문 원전, 인물·장소·사건 644개 엔티티 + 관계 667개짜리 지식그래프로 구성된다. Obsidian 볼트가 정본이고 이 레포는 자동 동기화 없는 수동 사본이다 — 볼트 쪽 변경을 이 레포가 자동으로 따라가지 않는다.

## 폴더 구조

| 경로 | 내용 |
|---|---|
| `points/*.md` | 34개. 『30포인트로 읽어내는 로마 제국 쇠망사』(가나모리 시게나리 편역, 북프렌즈) 한국어판 — 30포인트 + 일러두기·책머리에·옮기고나서·목차. 각 파일에 `### 등장 객체` 절. 본문 위키링크는 부분적으로만 박혀 있다 |
| `source/*.md` (+ `.txt` 쌍) | 72개. 기번 영문 원전 71장 + 서문 (Project Gutenberg #25717, 퍼블릭 도메인) |
| | **`points/`는 시판 편역본의 재서술이라 편데 팀 운영 범위에서만 쓴다. `source/`는 퍼블릭 도메인이라 제약이 없다** |
| `chapters/*.html` | 2개. 한영 대조 번역. 71장 중 2장만 완료, 계속 늘어난다 |
| `ontology/entities.jsonl` | 644줄. `{id, name, type, aliases, attrs, points, descs, note}` |
| `ontology/links.jsonl` | 667줄. `{from, to, rel, point, from_year, to_year, year_basis}` |
| `ontology/chronology.csv` | 82행 |
| `entities/<type>/<이름>.md` | 644개. person 262 · place 224 · group 66 · event 46 · institution 23 · work 15 · period 6 |
| `ontology/_scripts/rome30_*.py` | 온톨로지 재생성 스크립트 |
| `family/` | 족보 5계열 |
| `visual/` | 해설 자료 |

## 무엇을 하려는지별 진입점

- **온톨로지 데이터를 조회·분석**한다 → `ontology/entities.jsonl`, `ontology/links.jsonl`, `ontology/chronology.csv`를 직접 파싱
- **특정 인물·장소·사건 노트를 읽는다** → `entities/<type>/<이름>.md`
- **30포인트 편역본 본문을 인용한다** → `points/*.md` (한국어)
- **기번 원전을 인용한다** → `source/*.md` 또는 `.txt` (영어)
- **한영 대조를 본다** → `chapters/*.html` (2장뿐이니 없으면 없는 게 정상)
- **온톨로지를 재생성·확장한다** → `ontology/_scripts/rome30_*.py`. **`rome30_merge.py`는 절대 다시 돌리지 않는다** — 아래 함정 참조
- **족보·시각자료를 본다** → `family/`, `visual/`

## 불변식 여섯 (깨지면 되돌린다)

1. 객체 하나에 노트 하나. `entities.jsonl`의 모든 객체는 `entities/<type>/<이름>.md`를 갖는다. 동명이 타입이 겹칠 때만 파일명에 접미사(`그리스 (지명).md` · `그리스 (집단).md`)를 붙이고, jsonl 쪽 이름에는 접미사를 붙이지 않는다
2. rel은 16종만 쓴다: `child_of` `succeeded` `allied_with` `opposed` `participated_in` `occurred_at` `ruled` `member_of` `married` `conquered` `created` — 그리고 2026-08-13 신설분 `located_in`(인물·집단·장소 → 장소, 체류·소재·포함) `protected`(보호자 → 피보호자, 비대칭) `held_office`(인물 → 제도, 임기는 연도 필드에) `decided`(인물·제도 → 사건, 표결이지 참전이 아니다) `applied_to`(제도·정책 → 장소·집단)
3. 기원전은 음수, 서기는 양수
4. `occurred_at`의 주어는 항상 event다
5. `participated_in`의 방향은 인물·집단 → 사건이다
6. 끊어진 링크 0. `from` · `to`는 모두 실재하는 id여야 한다

## 함정 열둘 (전부 실제로 밟은 것)

- **원전이 두 계통이다.** `points/`(30포인트 편역본)와 `source/`·`chapters/`(기번 원전)는 다른 책이다. 섞어 인용하면 사실관계가 어긋난다. 온톨로지는 30포인트 계통에서만 뽑았으므로, 원전 인용이 필요한 작업에서는 온톨로지 커버리지를 기대하지 마라.
- **편역본 자체에 오류가 있다.** 포인트 01·02 본문 45건을 사료와 대조해 심각 6건을 찾았다 — 전투 승패가 뒤집힌 서술이 두 장에 하나씩, 메시나·밀라이 지휘관 인명 혼동, 레굴루스 고문사는 폴리비오스에 없는 후대 전설. 편역본 서술을 그대로 사실로 취급하지 마라.
- **동명이인을 설명 유사도로 판정하면 안 된다.** 카이사르처럼 여러 포인트에 걸쳐 서술된 인물은 같은 사람인데도 설명 유사도가 낮게 나온다. alias 일치도 못 믿는다 — 실제로 12쌍 중 4쌍이 서로 다른 인물이었다.
- **AI의 분류·연도 추정·인과 판단은 오류율이 높다.** 제안 68건 중 19건(약 28%)을 반려했다. 반대로 좌표·본문 추출 같은 기계적 작업은 대체로 정확했다 — 판단이 들어가는 작업과 추출 작업을 구분해서 신뢰도를 다르게 둬라.
- **`rome30_merge.py`를 다시 돌리면 안 된다.** 이건 최초 1회용 부트스트랩이고 이미 끝났다. 이 스크립트는 현재 `entities.jsonl`·`links.jsonl`을 **읽지 않는다** — `ontology/_parts/*.jsonl`만 읽어 두 파일을 통째로 덮어쓴다. `_parts/`는 2026-07-28에 멈춰 있고 연도 필드조차 없다(연도는 하류 `rome30_time.py`가 붙인다). 지금 `--write`로 돌리면 그 이후의 모든 수정이 사라진다. **지금 정본은 `entities.jsonl`·`links.jsonl`이고 `_parts/`는 유물이다.**
- **`rome30_geo.py`는 손으로 고친 좌표를 덮어쓴다.** 지명 사전으로 매번 다시 해석하기 때문에, 사람이 교정한 값이 있으면 원래의 오답으로 되돌아간다. 2026-08-13에 실제로 `본 곶`이 튀니지 케이프본(37.08, 11.02)에서 프랑스 불로뉴쉬르메르(50.72, 1.57)로 덮였다 — 사전이 `본 곶`을 `Bononia`로 오매칭한다. **`geo --write` 뒤에는 `entities/place/` diff를 반드시 눈으로 확인하고, 손교정 좌표가 되돌아갔으면 복구하라.** 지금 알려진 손교정 지명: `본 곶`.
- **하류 재생성은 안전하되 순서가 있다.** `geo → maps → routes → territory → backlink`. 전부 live jsonl을 읽어 `entities/*.md`를 다시 만든다. `backlink`가 되돌려 넣는 재료가 `routes`·`territory`의 산출물이라 그 뒤에 와야 한다. 순서를 건너뛰면 하류가 붙인 좌표·지도 섹션이 빠진 노트가 나온다.
- **스크립트의 `BASE`는 볼트 경로로 하드코딩돼 있다.** 레포 터미널에서 돌려도 레포의 `ontology/`·`entities/`는 바뀌지 않는다. 재생성 후 레포 반영은 수동 복사다.
- **rel 오용이 이미 쌓여 있다.** `occurred_at` 37건(주어가 event가 아님), 인물→지명 `participated_in` 20건. 표현할 rel이 없어서 생긴 것이라 2026-08-13에 `located_in` 등 5종을 신설했다. **마이그레이션은 진행 중이라 아직 옛 오용분이 남아 있다** — 조회할 때 양쪽을 다 봐야 하고, 새 링크를 만들 때 이 패턴을 답습하면 안 된다.
- **`child_of`는 이름과 반대로 읽어야 한다.** `X --child_of--> Y`는 "X가 Y의 자식"이 아니라 **"X의 자식이 Y"**다. 노트 생성기가 이 관례를 따라 X의 노트에 "자녀 → Y"로 찍는다. 영어 이름대로 읽고 방향을 뒤집으면 데이터와 생성기가 동시에 어긋난다 — 2026-08-13에 실제로 저지르고 되돌렸다. **방향을 "고치지" 마라.** 진짜 오류는 애초에 부모자식이 아닌 관계다(마리우스는 카이사르의 고모부이지 부자가 아니어서 삭제했다). 57건 전수 검증은 사료 대조가 필요해 미완이다.
- **이름의 괄호는 동명이인 구분 접미사다.** `율리아 (카이사르 가문)`은 가문이 아니라 **율리아라는 사람**이다. `그리스 (지명)`·`그리스 (집단)`과 같은 규칙이다(불변식 1). 접미사를 내용으로 오해하면 타입을 잘못 바꾸게 된다 — 이것도 같은 날 저지르고 되돌렸다.
- **없는 관계를 억지로 만들지 않는다.** 본문에 배경 언급만 있는 지명·인물에 억지로 링크를 다는 것보다 비워 두는 편이 낫다. 링크 개수를 늘리는 것 자체는 목표가 아니다.

## 함께 볼 것

- 데이터를 다루기 전에 볼트 쪽 운영 정본을 먼저 본다: `Admin/로마제국쇠망사_온톨로지_운영/운영_온톨로지_리포_동기화.md` (볼트에만 있고 이 레포에는 없다)
- 이 레포만 보고 최신이라 가정하지 마라. 볼트 쪽에 더 새로운 것이 있을 수 있다

## 스킬

`.agent/skills/<이름>/SKILL.md`에 있다. 각 스킬의 「하지 말 것」 절이 핵심이다 — 위 함정을 작업 단위로 풀어 놓은 것이다.

| 스킬 | 언제 |
|---|---|
| `point-context` | 포인트 하나를 준비할 때. 본문·등장 객체·관계를 한 덩어리로 묶어 AI에 넘긴다 |
| `entity-lookup` | 객체 하나의 전모를 볼 때. 설명·연도·관계·등장 포인트·좌표·노트 위치 |
| `relation-query` | 관계 조건으로 뽑을 때. 누가 누구와, 언제, 어떤 rel |
| `timeline-slice` | 연도 구간으로 사건·인물을 뽑을 때. 기원전 부호와 연도 미상 367건을 다룬다 |
| `fact-check` | 편역본 서술을 사료와 대조할 때 |
| `homonym-check` | 동명이인을 가를 때. 유사도·alias 판정 금지, 포인트별 서술 대조 |
| `propose-change` | 온톨로지에 무언가를 더하거나 고치자고 제안할 때. 직접 고치지 않는다 |
| `regenerate` | 스크립트 파이프라인을 돌릴 때. `merge` 금지와 순서 강제 |
